# AGENTS.md — دستورالعملِ دائمیِ ایجنتِ تولید (TA-TEEL-MV)

تو ایجنتی هستی با قابلیتِ تولیدِ تصویر، که به این ریپو وصل شده‌ای تا موزیک‌ویدیوی استاپ‌موشنِ ترانه‌ی «تعطیل (برقِ من)» و بازیِ آن را بسازی. این فایل را قبل از هر کاری کامل بخوان و قوانینش را law بگیر. نسخه: ۲۵ سپتامبر ۲۰۲۶ — پس از سازمان‌دهیِ ریپو.

## ۱) منبعِ حقیقت: خودِ آهنگ
فایلِ **`BARGH (4).mp3`** در ریشه‌ی ریپو، تنها مرجعِ رسمیِ طول و تمپو است.
- **هیچ عددی را از هیچ سندی بدونِ سنجش قبول نکن.** اعدادِ ۴:۱۸ و ۱۰۵ BPM در داکیومنت‌ها پیش‌فرضِ پیش‌تولید بودند، نه حکم.
- قدمِ اول: طولِ دقیق و BPM و شبکه‌ی ضربِ فایل را **بسنج** (ffprobe / librosa / هر ابزارِ در دسترسِ runtime). اگر ابزار نداری، از کاربر بپرس — **حدس و گمان مطلقاً ممنوع.**
- بعد `docs/timing-map.md` (۳۶ گام) را با اعدادِ اندازه‌گیری‌شده کالیبره کن و با تأییدِ کاربر قفل کن. اگر اختلاف جدی است، **آهنگ برنده است.**

## ۲) نقشه‌ی ریپو (سازمان‌یافته — ۲۵ سپتامبر ۲۰۲۶)
- **ریشه:** `BARGH (4).mp3` (منبعِ حقیقت) · `AGENTS.md` (همین) · `README.md` · `game-design.md` (سندِ بازی) · `animatic.html` (مرجعِ چشمی: دوربینِ از بالا، ۳۶ گام، ۹ لوکیشن) · `.gitignore`
- `docs/` — `style-bible.md` (قوانینِ سبک + قانونِ دوربین) · `master-prompt.md` (رسمی) · `lyrics-v7.md` (شعر — بایت‌به‌بایت، دست‌نخورده) · `song-doc.md` · `storyboard.md` · `gta-pipeline.md` · `production-plan.md` (صف و بودجه‌ی فریم) · `timing-map.md` (۳۶ گام)
- `prompts/anchors.md` — لنگرِ هر سکانس + بلوکِ سبک/منفی + جمله‌ی قفلِ دوربین
- `refs/characters/` (۹ پرتره‌ی pro) · `refs/style/` (لنگرِ سبکِ آیکونِ صدام + نسخه‌ی آلفادارِ سنجش) · `refs/locations/` (۹ مرجعِ چیدمان = اسکرین‌شاتِ انیماتیک) · `refs/scenes/` (پلیت‌های مینیاتوریِ مرجع)
- `frames/` — ⚠️ **تولیدِ زنده:** `00-pilot-candle/` (استانداردِ پایلوت) + `01-event-studio/` (در کار) + فریم‌های ریشه‌ی `F*.jpg` (زنجیره‌ی جاری). **هیچ فایلی در frames/ هرگز جابه‌جا، بازنویسی یا حذف نمی‌شود.** نام‌گذاری: `F{n}.jpg` / جایگزینی `F{n}r.jpg` / رد‌شده `-REJECTED.jpg`
- `logs/` — ⚠️ **تولیدِ زنده:** `INDEX.md` (نمایه‌ی کارِ انجام‌شده) · `_TEMPLATE.md` (قالبِ لاگِ هر شات) · لاگ‌های جاریِ `01-event-*`. لاگِ تازه با همان الگو.
- `game-assets/` — دارایی‌های بازی: `icons/` (۱۰ آیکون + `ICONS.md` + `PROMPTS-ICONS.md`) · `hud/` (۳ قاب) · `statics/props/` (۸ استاتیکِ از بالا) · `locations/` (۹ پلیتِ نقشه + شیتِ بازبینی) · `hud-demo.html` (دموی زنده‌ی ویجت‌ها) · `ASSETS.md` (رجیستری + مشخصاتِ رفتاریِ ویجت‌ها) · `PROMPTS-GAME-ASSETS.md`
- `tools/` — `align_frames.py` (گِیتِ قانونیِ هم‌ترازی: هشِ ۸×۸، آستانه‌ها لوپ ≤۱۲ / پیوسته ≤۲۵) · `build_shot.py` (مونتاژ: `shots/*.json` → گیف + sidecar) · `build_animatic.py` (سازنده‌ی انیماتیک — فقط با refs محلی کار می‌کند)
- `archive/` — تاریخچه‌ی قدیمی (HTMLهای آرشیوی، READMEهای قبلی، draftها). فقط خواندنی؛ مرجع نیست مگر با ذکرِ صریح.
- `shots/` — خروجی‌های مونتاژ (در زمانِ نیاز ساخته می‌شود)

## ۳) قوانینِ شکست‌ناپذیر
- **دوربین:** در «همه‌ی» نماها از بالا، عمود، نورت‌آپ — مثلِ GTA کلاسیک (GTA 1/2). هیچ نمای جانبی، سه‌رخ یا ایزومتریک. قفلِ قاب در هر پرامپت:
  `top-down bird's-eye view, camera looking straight down, north-up, classic GTA 1/2 perspective — no side view, no isometric`
- **پرامپتِ تصویر فقط انگلیسی** — هیچ Persian script داخلِ پرامپتِ تصویر نمی‌رود. شعر و متن‌های فارسی فقط در داکیومنت‌ها و لاگ‌ها، بایت‌به‌بایت و دست‌نخورده.
- **امضای مینیاتورِ صفوی** در ریزترین جزئیاتِ هر فریم و هر دارایی (الگو: گوشه‌ی تذهیبِ حیاطِ مجلس در `animatic.html`، پلاکِ درِ نیروگاه).
- **تمِ مینیاتوری:** دارایی‌های تازه (پلیت، استاتیک، HUD) باید زبانِ نگارگریِ صفوی داشته باشند — لاجوردِ کانی، طلاورقِ تذهیب، بندِ ابری، عمقِ تختِ تزئینی. مرجع: `refs/scenes/` و `refs/style/`؛ نه نقاشیِ دیجیتالِ معمولی.
- **قاب‌های گیف:** کادر و ترکیب‌بندی در جای و اندازه‌ی دقیقاً همان‌جا؛ لوپِ تنگ مستقیم از خودِ لنگر + جمله‌ی قفلِ فریم + گِیتِ `tools/align_frames.py`.
- **هیچ فایلی هرگز حذف یا بازنویسی نمی‌شود** — به‌ویژه در `frames/` و `logs/` (تولیدِ زنده).
- هر فریم/شات: پرامپتِ دقیقِ استفاده‌شده آرشیو شود + sidecar جیسون + لاگ با قالبِ `logs/_TEMPLATE.md`.
- قبل از هر تولیدِ انبوه: **یک فریمِ آزمایشی نشان بده، تأیید بگیر، بعد ادامه بده.**

## ۴) دو خطِ مأموریت (به‌دستورِ کاربر)
- **بازی (مأموریتِ اصلی — `game-design.md`):** موزیک‌ویدیوی قابلِ بازیِ GTA کلاسیک از بالا؛ نقاطِ عطفِ M0–M4؛ خروجی `game.html`. دارایی‌ها آماده‌اند در `game-assets/`.
- **موزیک‌ویدیوی فریم‌به‌فریم (`docs/production-plan.md`):** تولیدش **از قبل شروع شده** — `frames/01-event-studio/` و لاگ‌های `logs/01-event-*` را بخوان و از همان‌جا ادامه بده؛ چیزهایی که شده را دوباره نساز.
- اولویت را در هر نوبت از کاربر بپرس.

## ۵) تعریفِ «تمام» برای هر شاتِ MV
۱) همه‌ی فریم‌ها از گِیتِ هم‌ترازی گذشته (لوپ ≤۱۲، پیوسته ≤۲۵) · ۲) کانتکت‌شیت ساخته و به کاربر نشان داده شده · ۳) گیف با `tools/build_shot.py` + sidecar · ۴) لاگِ کامل · ۵) زمانِ شوت با تایم‌مپِ کالیبره‌شده‌ی آهنگ خوانا بوده باشد.

## ۶) کمبودها
`assets/fonts/Lalezar-Regular.ttf` هنوز در ریپو نیست (فونتِ فارسیِ متنِ درونِ بازی/HUD؛ تا آپلود شود: fallback با ذکر در گزارش).

## ۷) پروتکلِ گزارش
در پایانِ هر نوبت: چه ساختی، کجا ذخیره شد، چه گِیت‌هایی گذشت، بعدیِ صف چیست. چیزی را که نمی‌دانی از کاربر بپرس — در تولیدِ تصویر، حدسِ خلاقانه ممنوع است.
